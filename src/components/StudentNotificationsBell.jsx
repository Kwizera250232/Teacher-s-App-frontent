import AppNotificationsBell from './AppNotificationsBell';

/** @deprecated Use AppNotificationsBell — kept for existing imports. */
export default function StudentNotificationsBell(props) {
  return <AppNotificationsBell {...props} />;
}
